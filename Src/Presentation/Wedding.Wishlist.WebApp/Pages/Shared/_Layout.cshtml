<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <title>@ViewData["Title"] - Wishlist Casamento</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

    <!-- Bootstrap -->
    <link href="~/lib/bootstrap/css/bootstrap.min.css" rel="stylesheet" />

    <!-- SweetAlert -->
    <link rel="stylesheet" href="~/lib/sweetalert2/sweetalert2.min.css" />

    <!-- Site CSS -->
    <link href="~/css/site.css" rel="stylesheet" />
</head>
<body>

    <header class="navbar navbar-expand-lg bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand fw-semibold" asp-page="/Index">
                💍 Nossa Lista
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div id="menu" class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto align-items-lg-center gap-lg-3">

                    <li class="nav-item">
                        <a class="nav-link" asp-page="/Index">Início</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" asp-page="/Wishlist/Gifts">Presentes</a>
                    </li>

                    @if (!User.Identity?.IsAuthenticated ?? true)
                    {
                        <li class="nav-item">
                            <a class="btn btn-outline-primary btn-sm px-3" asp-page="/Login">
                                Entrar
                            </a>
                        </li>

                        <li class="nav-item">
                            <a class="btn btn-primary btn-sm px-3" asp-page="/Register">
                                Criar conta
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <span class="nav-link">
                                Olá, <strong>@User.Identity!.Name</strong>
                            </span>
                        </li>

                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link text-danger fw-semibold" asp-page="/Admin/Index">
                                    Administração
                                </a>
                            </li>
                        }

                        <li class="nav-item">
                            <a class="btn btn-outline-danger btn-sm px-3" asp-page="/Logout">
                                Sair
                            </a>
                        </li>
                    }

                </ul>
            </div>
        </div>
    </header>

    <div id="globalLoading"
         class="loading-overlay d-none">
        <div class="loading-content text-center">
            <div class="spinner-border text-primary mb-3" role="status"></div>
            <div class="fw-semibold text-primary">Carregando...</div>
        </div>
    </div>

    <main class="container my-5">
        @RenderBody()
    </main>

    <footer class="text-center text-muted py-4">
        <small>Feito com ❤️ para nosso casamento</small>
    </footer>

    <script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/sweetalert2/sweetalert2.all.min.js"></script>
    <script src="~/js/site.js"></script>
    @RenderSection("Scripts", required: false)

</body>
</html>
